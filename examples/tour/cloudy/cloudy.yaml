tosca_definitions_version: tosca_2_0

node_types:

  Server:
    capabilities:
      data_endpoint: Endpoint
      admin_endpoint: Endpoint

capability_types:

  Endpoint:
    properties:
      protocol:
        type: string
      port:
        type: integer
